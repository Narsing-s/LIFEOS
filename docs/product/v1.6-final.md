# LIFEOS V1.6 final scope

The connected-life release is intentionally production-safe: credentials are encrypted, sync is durable, provider imports are bounded, and the UI exposes connection/sync state instead of pretending external data exists before a provider is configured.

Enabled imports: Calendar, Drive metadata, Gmail metadata.

Planned adapters: Photos, Contacts.
