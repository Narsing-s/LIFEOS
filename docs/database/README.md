# LIFEOS Database

PostgreSQL is the source of truth. Prisma owns the relational schema; pgvector supports semantic document retrieval. User-owned tables are keyed by user_id and should receive appropriate indexes and row-level security in production.
