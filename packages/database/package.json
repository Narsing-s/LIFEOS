{
  "name": "@lifeos/database",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "build": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lifeos prisma validate",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "typecheck": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lifeos prisma validate"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1"
  },
  "devDependencies": {
    "prisma": "^6.3.1",
    "typescript": "^5.7.3",
    "vitest": "^5.0.1"
  }
}
